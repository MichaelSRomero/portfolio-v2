import * as React from "react";

import "./navbar.scss";

const Navbar = ({logo, navItems}) => {
  return (
    <nav className="is-flex">
      {/* Insert Logo */}
      <ul>
        {/* Insert Items */}
      </ul>
    </nav>
  )
}

export default Navbar;